name: iwipe
description: CAP project - Iwipe Cleaning App Training platform
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
    sdk: '>=3.1.0 <4.0.0'

dependencies:
    flutter:
        sdk: flutter

    flutter_bloc: ^8.1.2
    flutter_screenutil:
    dots_indicator:
    cloud_firestore:





    flutter_localizations:
        sdk: flutter
    intl: ^0.18.1
    cupertino_icons: ^1.0.2

dev_dependencies:
    flutter_test:
        sdk: flutter
    flutter_lints: ^2.0.0

flutter:
    uses-material-design: true
    generate: true
    assets:
        - assets/
        - assets/images/
        - assets/icons/
        - assets/svg/
        - assets/videos/
